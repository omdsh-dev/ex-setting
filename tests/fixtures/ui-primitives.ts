/** Minimal test seam for locale's unrelated settings-row presentation dependency. */
export function IconChevronDownOutline14(): null {
  return null
}

export function Menu(props: { anchor?: unknown }): unknown {
  return props.anchor ?? null
}
