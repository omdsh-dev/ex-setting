# Bilingual-pair consistency record for the crawler protocol documents: the git blob hash of each
# side as of the last confirmed-consistent state. Both languages carry equal authority;
# after editing either side, bring the other along and re-record with:
#   Edit both `docs/web-config-crawler.md` and `docs/web-config-crawler.zh.md`, then
#   refresh the two hashes below with `git hash-object`.
web-config-crawler.md: 3407aa71235a40045dcfcb6e82cce1dd080ad6ec
web-config-crawler.zh.md: 802c6133ec7f8d3026498067bfbd726b76c3e410
